{
  "name": "vercel-docker",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose up -d && docker-compose run --rm panel php artisan p:user:make",
    "vercel-build": "echo 'Building on Vercel'"
  }
}
